# Database (PostgreSQL)
# For local Docker Compose: not needed - compose sets these automatically.
# For local PostgreSQL on your machine:
#   PGHOST=localhost
#   PGPORT=5432
#   PGUSER=taskuser
#   PGPASSWORD=taskpass
#   PGDATABASE=task_tracker
# For AWS RDS: use your RDS endpoint and credentials.

PGHOST=localhost
PGPORT=5432
PGUSER=taskuser
PGPASSWORD=taskpass
PGDATABASE=task_tracker

# API server
PORT=3000

# Optional: set to "development" for detailed error messages in responses
# NODE_ENV=production